# function fishlist -d "list operations"
# 	set -l subcommand $argv[1]
# 	switch ($subcommand)
# 	case dropleft
# 		set -l n $argv[2]
# 		set -l i 1
# 		for a in $argv 
		
# 		for i in (seq 1 )
# 			set -l a $argv[i]
# 			if test -z ()
# end