function fish_user_key_bindings
	bind \ef accept-autosuggestion and execute or complete
	bind \ew backward-kill-word
	bind \eq backward-kill-bigword
	# bind \ee backward-jump #opens EDITOR
	# bind \eE forward-jump
	bind \ez undo
	bind \eZ redo
end